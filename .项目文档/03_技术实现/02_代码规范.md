# 代码规范

## 总体规范

### 1. 代码风格
- 使用统一的代码格式化工具
- 遵循阿里巴巴Java开发手册
- 使用ESLint规范前端代码
- 保持代码简洁、可读性强

### 2. 命名规范
- 使用有意义的变量名和函数名
- 遵循驼峰命名法
- 常量使用全大写字母
- 避免使用缩写和拼音

### 3. 注释规范
- 类和方法必须有完整的注释
- 复杂逻辑必须添加行内注释
- 使用JSDoc规范前端注释
- 使用JavaDoc规范后端注释

## 前端代码规范

### 1. Vue组件规范

#### 1.1 组件命名
```javascript
// 组件名使用PascalCase
export default {
  name: 'KosListManagement'
}

// 文件名使用kebab-case
// kos-list-management.vue
```

#### 1.2 组件结构
```vue
<template>
  <!-- 模板内容 -->
</template>

<script setup>
// 导入
import { ref, reactive, computed, onMounted } from 'vue'
import { ElMessage } from 'element-plus'

// 响应式数据
const loading = ref(false)
const tableData = ref([])

// 计算属性
const filteredData = computed(() => {
  // 计算逻辑
})

// 方法
const handleSearch = () => {
  // 方法实现
}

// 生命周期
onMounted(() => {
  // 初始化逻辑
})
</script>

<style scoped>
/* 样式内容 */
</style>
```

#### 1.3 Props定义
```javascript
// 使用defineProps定义props
const props = defineProps({
  // 基础类型检查
  title: {
    type: String,
    required: true
  },
  // 多种可能的类型
  data: {
    type: [Array, Object],
    default: () => []
  },
  // 自定义验证函数
  status: {
    type: String,
    validator: (value) => ['active', 'inactive'].includes(value)
  }
})
```

### 2. JavaScript规范

#### 2.1 变量声明
```javascript
// 使用const声明常量
const API_BASE_URL = 'https://api.example.com'

// 使用let声明可变变量
let currentPage = 1

// 避免使用var
// var oldVariable = 'avoid this'
```

#### 2.2 函数定义
```javascript
// 使用箭头函数
const fetchData = async () => {
  try {
    const response = await api.getData()
    return response.data
  } catch (error) {
    console.error('获取数据失败:', error)
    throw error
  }
}

// 使用async/await处理异步
const handleSubmit = async (formData) => {
  loading.value = true
  try {
    await api.submitData(formData)
    ElMessage.success('提交成功')
  } catch (error) {
    ElMessage.error('提交失败')
  } finally {
    loading.value = false
  }
}
```

#### 2.3 对象和数组操作
```javascript
// 使用解构赋值
const { name, age, email } = user

// 使用展开运算符
const newUser = { ...user, status: 'active' }

// 使用数组方法
const activeUsers = users.filter(user => user.status === 'active')
const userNames = users.map(user => user.name)
```

### 3. CSS规范

#### 3.1 类名规范
```css
/* 使用BEM命名规范 */
.kos-list {
  /* 块 */
}

.kos-list__header {
  /* 元素 */
}

.kos-list__header--active {
  /* 修饰符 */
}
```

#### 3.2 样式组织
```css
/* 按功能分组 */
/* 布局样式 */
.layout-container {
  display: flex;
  flex-direction: column;
}

/* 组件样式 */
.table-wrapper {
  margin: 16px 0;
}

/* 工具样式 */
.text-center {
  text-align: center;
}
```

## 后端代码规范

### 1. Java代码规范

#### 1.1 类命名
```java
// 类名使用PascalCase
public class KosListController {
    // 类内容
}

// 接口名以I开头
public interface IKosService {
    // 接口内容
}

// 实现类以Impl结尾
public class KosServiceImpl implements IKosService {
    // 实现内容
}
```

#### 1.2 方法命名
```java
// 方法名使用camelCase
public List<KosInfo> getKosList(KosQuery query) {
    // 方法实现
}

// 布尔方法使用is/has/can开头
public boolean isKosActive(String userId) {
    // 方法实现
}

// 私有方法使用下划线开头
private void _validateKosData(KosInfo kosInfo) {
    // 方法实现
}
```

#### 1.3 变量命名
```java
// 常量使用全大写
public static final String DEFAULT_PAGE_SIZE = "20";

// 成员变量使用camelCase
private String brandId;
private List<KosInfo> kosList;

// 局部变量使用camelCase
String userName = "张三";
int totalCount = 0;
```

### 2. 注解规范

#### 2.1 类注解
```java
@RestController
@RequestMapping("/api/kos")
@Api(tags = "KOS管理")
@Slf4j
public class KosListController {
    // 类内容
}
```

#### 2.2 方法注解
```java
@GetMapping("/list")
@ApiOperation("获取KOS列表")
public Result<List<KosInfo>> getKosList(
    @ApiParam("查询条件") @RequestParam(required = false) String brandId,
    @ApiParam("页码") @RequestParam(defaultValue = "1") Integer pageNum,
    @ApiParam("页大小") @RequestParam(defaultValue = "20") Integer pageSize
) {
    // 方法实现
}
```

#### 2.3 参数验证
```java
@PostMapping("/add")
public Result<Void> addKos(@Valid @RequestBody KosAddRequest request) {
    // 方法实现
}

// 请求对象
public class KosAddRequest {
    @NotBlank(message = "品牌ID不能为空")
    private String brandId;
    
    @NotBlank(message = "用户ID不能为空")
    private String userId;
    
    @Size(max = 100, message = "昵称长度不能超过100个字符")
    private String nickname;
}
```

### 3. 异常处理规范

#### 3.1 自定义异常
```java
// 业务异常
public class BusinessException extends RuntimeException {
    private String code;
    private String message;
    
    public BusinessException(String code, String message) {
        super(message);
        this.code = code;
        this.message = message;
    }
}

// 使用示例
if (StringUtils.isBlank(brandId)) {
    throw new BusinessException("BRAND_ID_EMPTY", "品牌ID不能为空");
}
```

#### 3.2 全局异常处理
```java
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        log.error("业务异常: {}", e.getMessage(), e);
        return Result.error(e.getCode(), e.getMessage());
    }
    
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        log.error("系统异常: {}", e.getMessage(), e);
        return Result.error("SYSTEM_ERROR", "系统异常，请联系管理员");
    }
}
```

### 4. 数据库操作规范

#### 4.1 Mapper接口
```java
@Mapper
public interface KosListMapper extends BaseMapper<KosList> {
    
    /**
     * 分页查询KOS列表
     * @param query 查询条件
     * @return KOS列表
     */
    List<KosList> selectKosListPage(@Param("query") KosQuery query);
    
    /**
     * 批量更新KOS状态
     * @param userIds 用户ID列表
     * @param status 状态
     * @return 更新数量
     */
    int batchUpdateStatus(@Param("userIds") List<String> userIds, @Param("status") Integer status);
}
```

#### 4.2 SQL规范
```xml
<!-- 使用参数化查询 -->
<select id="selectKosListPage" resultType="com.kos.entity.KosList">
    SELECT 
        brand,
        brand_id,
        user_id,
        nickname,
        avatar,
        sort_order,
        owner_user,
        owner_shop,
        channel,
        participate_statistics,
        az_batch_no
    FROM 配置_小红书专业号_kos列表
    <where>
        <if test="query.brandId != null and query.brandId != ''">
            AND brand_id = #{query.brandId}
        </if>
        <if test="query.channel != null and query.channel != ''">
            AND channel = #{query.channel}
        </if>
        <if test="query.status != null">
            AND participate_statistics = #{query.status}
        </if>
    </where>
    ORDER BY sort_order ASC, create_time DESC
</select>
```

## 文档规范

### 1. API文档
```java
@Api(tags = "KOS管理")
@RestController
@RequestMapping("/api/kos")
public class KosListController {
    
    @ApiOperation("获取KOS列表")
    @GetMapping("/list")
    public Result<PageResult<KosList>> getKosList(
        @ApiParam("品牌ID") @RequestParam(required = false) String brandId,
        @ApiParam("页码") @RequestParam(defaultValue = "1") Integer pageNum,
        @ApiParam("页大小") @RequestParam(defaultValue = "20") Integer pageSize
    ) {
        // 方法实现
    }
}
```

### 2. 代码注释
```java
/**
 * KOS列表管理服务
 * 
 * @author 开发者姓名
 * @version 1.0
 * @since 2024-01-01
 */
@Service
@Slf4j
public class KosListService {
    
    /**
     * 获取KOS列表
     * 
     * @param query 查询条件
     * @return KOS列表
     * @throws BusinessException 业务异常
     */
    public List<KosList> getKosList(KosQuery query) throws BusinessException {
        // 方法实现
    }
}
```

### 3. 变更日志
```markdown
## 变更日志

### [1.0.0] - 2024-01-01
#### 新增
- KOS列表管理功能
- KOS销售数据管理功能
- Excel导入导出功能

#### 修复
- 修复分页查询问题
- 修复数据验证问题

#### 优化
- 优化查询性能
- 优化用户体验
```

## 测试规范

### 1. 单元测试
```java
@SpringBootTest
class KosListServiceTest {
    
    @Autowired
    private KosListService kosListService;
    
    @Test
    @DisplayName("测试获取KOS列表")
    void testGetKosList() {
        // 准备测试数据
        KosQuery query = new KosQuery();
        query.setBrandId("001");
        
        // 执行测试
        List<KosList> result = kosListService.getKosList(query);
        
        // 验证结果
        assertThat(result).isNotNull();
        assertThat(result.size()).isGreaterThan(0);
    }
}
```

### 2. 集成测试
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
class KosListControllerIntegrationTest {
    
    @Autowired
    private TestRestTemplate restTemplate;
    
    @Test
    @DisplayName("测试KOS列表API")
    void testGetKosListApi() {
        // 执行测试
        ResponseEntity<Result> response = restTemplate.getForEntity(
            "/api/kos/list?brandId=001", Result.class);
        
        // 验证结果
        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(response.getBody().getCode()).isEqualTo("200");
    }
}
```

## 版本控制规范

### 1. Git提交规范
```bash
# 提交格式
<type>(<scope>): <subject>

# 示例
feat(kos): 添加KOS列表管理功能
fix(api): 修复分页查询问题
docs(readme): 更新项目文档
style(ui): 优化界面样式
refactor(service): 重构业务逻辑
test(unit): 添加单元测试
chore(deps): 更新依赖版本
```

### 2. 分支管理
```bash
# 主分支
main          # 生产环境分支
develop       # 开发环境分支

# 功能分支
feature/kos-management    # KOS管理功能
feature/sales-data        # 销售数据功能

# 修复分支
hotfix/fix-pagination     # 修复分页问题
```

### 3. 代码审查
- 所有代码必须经过代码审查
- 审查重点：代码质量、安全性、性能
- 使用Pull Request进行代码合并
- 审查通过后才能合并到主分支
